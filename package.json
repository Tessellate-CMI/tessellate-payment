{
    "name": "tessellate-payment",
    "version": "0.1.0",
    "module": "src/index.ts",
    "scripts": {
        "dev": "bun run --watch src/index.ts",
        "build": "bun build --target=bun --minify --outfile=server.js src/index.ts",
        "format": "prettier --write .",
        "commit": "prettier --check . && cz"
    },
    "dependencies": {
        "@directus/sdk": "^13.0.1",
        "@elysiajs/html": "^0.7.3",
        "dateformat": "^5.0.3",
        "elysia": "^0.7.21",
        "env-var": "^7.4.1",
        "sha256": "^0.2.0"
    },
    "devDependencies": {
        "bun-types": "^1.0.8",
        "commitizen": "^4.3.0",
        "cz-conventional-changelog": "^3.3.0",
        "eslint": "^8.52.0",
        "prettier": "^3.0.3"
    },
    "config": {
        "commitizen": {
            "path": "cz-conventional-changelog"
        }
    }
}
